{
  "scripts": {
    "lint": "chmod +x ./scripts/spectral.sh && ./scripts/spectral.sh",
    "bundle": "cd documents && npm i && npm run bundle"
  },
  "dependencies": {
    "@stoplight/spectral-cli": "^6.3.0"
  }
}
